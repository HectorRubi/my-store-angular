import { NumericVocalsPipe } from './numeric-vocals.pipe';

describe('NumericVocalsPipe', () => {
  it('create an instance', () => {
    const pipe = new NumericVocalsPipe();
    expect(pipe).toBeTruthy();
  });
});
